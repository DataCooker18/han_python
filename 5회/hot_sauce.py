def hot_sum (a, b, c):
    total_sum = a + b + c
    return total_sum
    
    
def hot_mul(a, b, c):
    total_mul = a * b * c
    return total_mul

def hot_avg(a, b, c):
    total_sum = a + b + c
    return total_sum/3

